# ============================================================
# Pearls AQI Predictor — dashboard runtime (slim)
# ============================================================
# Used by Streamlit Community Cloud.
#
# The dashboard is now a thin client that calls the Flask backend
# (deployed on Render) over HTTP. No direct Hopsworks or model
# dependencies needed here — those live in requirements-backend.txt.
#
# Versions match local dev environment.

# Core data / numeric
pandas==2.2.3
numpy==1.26.4

# Frontend
streamlit==1.57.0
plotly==6.7.0
altair==6.1.0

# HTTP client for backend calls
requests==2.33.1

# Config / secrets loading
python-dotenv==1.2.2