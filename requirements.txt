# ============================================================
# Pearls AQI Predictor — dashboard runtime (slim)
# ============================================================
# Used by Streamlit Community Cloud. Excludes training-only
# packages (TF, XGBoost, SHAP) and dev tooling (pytest, flask).
#
# Versions match local dev environment.

# Core data / numeric
pandas==2.2.3
numpy==1.26.4
pyarrow==24.0.0

# Feature store (read-only access)
hopsworks==4.8.1

# ML — only what's needed to LOAD pickled models
scikit-learn==1.5.2

# Frontend
streamlit==1.57.0
plotly==6.7.0

# Plotting backend that Streamlit needs
altair==6.1.0

python-dotenv==1.2.2