# ============================================================
# Pearls AQI Predictor — backend runtime (Render)
# ============================================================
# Slim dependencies for the Flask API. Excludes Streamlit,
# Plotly, TensorFlow, XGBoost, SHAP, and dev tooling.
# 
# Versions match local dev environment.

# Core data / numeric
pandas==2.2.3
numpy==1.26.4
pyarrow==24.0.0

# Feature store (read-only access)
hopsworks==4.8.1

# ML — only what's needed to LOAD pickled models
scikit-learn==1.5.2

# Backend framework
flask==3.1.3
gunicorn==23.0.0

# Config / secrets loading
python-dotenv==1.2.2